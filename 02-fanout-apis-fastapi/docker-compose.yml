version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: fanout_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - pgdata_fanout:/var/lib/postgresql/data
  api:
    build: .
    depends_on:
      - db
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/fanout_db
    ports:
      - "8000:8000"
volumes:
  pgdata_fanout:
