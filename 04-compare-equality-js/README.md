# 04-compare-equality-js

## Objetivo
Explicar a diferença entre `==` e `===` em JavaScript.

## Diferença
- `==` compara valores após conversão de tipo.
- `===` compara valor e tipo.

## Exemplo
```js
console.log(1 == '1');
console.log(1 === '1');
```
